module xnor_ic_74266(

    input  a1,b1,a2,b2,a3,b3,a4,b4,
    output f1, f2,f3, f4
   );
    assign f1 = ~(a1 ^ b1);
    assign f2 = ~(a2 ^ b2);
    assign f3 = ~(a3 ^ b3);
    assign f4 = ~(a4 ^ b4);

endmodule

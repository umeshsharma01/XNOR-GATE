# XNOR-GATE
An XNOR  gate is a digital logic gate that performs the complement of the XOR operation.
It produces a high output (1) when the input signals are equal.
When the inputs are different the output becomes low (0).
The XNOR operation is represented as the complement of XOR.
